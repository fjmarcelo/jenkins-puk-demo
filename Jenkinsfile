pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo "Repo clonado correctamente"
                sh 'ls -la'
            }
        }
        stage('Build') {
            steps {
                echo "Aquí iría la compilación o el test"
            }
        }
    }
}
